use pumpkin_plugin_api::common::Locale;

/// A singleton that simplifies the translation
/// of messages which are dependent on a player's locale.
pub struct Translation;

pub const SUCCESSFULLY_ADDED_ARENA: &'static str = "Successfully added the arena {arena}.";

impl Translation {
    pub fn x() {
        // todo
    }
}
